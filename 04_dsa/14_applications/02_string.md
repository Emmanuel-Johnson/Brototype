# String

## ✅ Definition (1 line)

A string is a sequence of characters stored in contiguous memory
locations, used to represent text.

------------------------------------------------------------------------

## 🚀 3--5 Applications of String

-   Text processing (searching, editing, formatting text)
-   Pattern matching algorithms (like searching substrings)
-   Data validation (email, password, input checking)
-   Parsing and tokenization (splitting sentences, commands)
-   Implementing compilers and interpreters

------------------------------------------------------------------------

## 🔎 Brief Explanation (1--2 Applications)

### 1️⃣ Pattern Matching

Strings are used in algorithms like substring search
(e.g., checking if "cat" exists inside "concatenate").

This is important in search engines, plagiarism checkers, and DNA
matching.

### 2️⃣ Parsing & Tokenization

When a program reads input like:

    name=Emmanuel&age=22

It splits the string into meaningful parts.

This is heavily used in web development.